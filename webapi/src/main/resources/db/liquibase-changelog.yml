databaseChangeLog:
  - changeSet:
      id: 20210608-1
      author: CarloMicieli
      changes:
        - createTable:
            columns:
              - column:
                  constraints:
                    nullable: false
                    primaryKey: true
                    primaryKeyName: PK_users
                  name: username
                  type: VARCHAR(50)
              - column:
                  constraints:
                    nullable: false
                  name: password
                  type: VARCHAR(50)
              - column:
                  name: mail_address
                  type: VARCHAR(50)
            tableName: users